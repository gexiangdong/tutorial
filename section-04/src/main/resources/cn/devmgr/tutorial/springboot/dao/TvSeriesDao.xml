<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.devmgr.tutorial.springboot.dao.TvSeriesDao" >
  
  <insert id="insert" parameterType="cn.devmgr.tutorial.springboot.pojo.TvSeries" 
  			useGeneratedKeys="true" keyProperty="id">
    insert into tv_series (name, episode_count, original_release )
    		values (#{name}, #{episodeCount}, #{originalRelease} )
  </insert>
  
  <update id="update" parameterType="cn.devmgr.tutorial.springboot.pojo.TvSeries">
    update tv_series set name=#{name}, episode_count=#{episodeCount}, original_release=#{originalRelease} where id=#{id}
  </update>
  
</mapper>